server:
  port: 9020

spring:
  application:
    name: catalog-service
  data:
    mongodb:
      uri: mongodb+srv://<name>:<password>@bookhaven.qjestxj.mongodb.net/bookstore?retryWrites=true&w=majority&appName=bookhaven

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
